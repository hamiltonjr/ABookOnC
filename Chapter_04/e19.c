/**
 * Exercise 19.
 */
#include <stdio.h>
#include <stdlib.h>

/**
 * for version (using for (;;) instead while (1))
 */

int main()
{
    //while (1)
    for (;;)
    {
        printf("TRUE FOREVER\n");
    }

    return EXIT_SUCCESS;
}

